{% extends 'base_back.html.twig' %}

{% block title %}Promo{% endblock %}

{% block body %}
    <h1>Promo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ promo.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_promo_index') }}">back to list</a>

    <a href="{{ path('app_admin_promo_edit', {'id': promo.id}) }}">edit</a>

    {{ include('admin_promo/_delete_form.html.twig') }}
{% endblock %}
