{% extends 'base_back.html.twig' %}

{% block title %}Documents{% endblock %}

{% block body %}
    
<div style="background-color: #D1D1D1; min-height:100vh">
    <div class="container" style="max-width:50vw">
        <h1>Documents</h1>

    <a href="{{ path('app_documents_index') }}"class="btn btn-primary mb-3">Retour à la liste</a>

    <a href="{{ path('app_documents_edit', {'id': document.id}) }}"class="btn btn-success mb-3">Editer</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ document.nom }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ document.createdAt ? document.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('documents/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
