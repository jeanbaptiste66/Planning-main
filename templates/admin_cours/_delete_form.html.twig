<form method="post" action="{{ path('app_admin_cours_delete', {'id': cour.id}) }}" onsubmit="return confirm('Vous êtes sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
