{% extends 'base_back.html.twig' %}

{% block title %}Modifier Cours{% endblock %}

{% block body %}
<div style="background-color: #D1D1D1; min-height:100vh">
    <div class="container" style="max-width:50vw">
        <h1 style="text-align:center" class="pt-4">Modifier Cours</h1>
        <div class="d-flex justify-content-between py-2">
            <a href="{{ path('app_admin_cours_index') }}" class="btn btn-primary mb-4">Retour à la liste</a>
            {{ include('admin_cours/_delete_form.html.twig') }}
        </div>
        {{ include('admin_cours/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>
{% endblock %}
