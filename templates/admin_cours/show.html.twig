{% extends 'base_back.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
    <h1>Cours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour.id }}</td>
            </tr>
            <tr>
                <th>Module</th>
                <td>{{ cour.module }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_cours_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_cours_edit', {'id': cour.id}) }}">Modifier</a>

    {{ include('admin_cours/_delete_form.html.twig') }}
{% endblock %}
