{% extends 'base_back.html.twig' %}

{% block title %}Centre{% endblock %}

{% block body %}
    <h1>Centre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ centre.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ centre.adresse }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ centre.codePostal }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ centre.mail }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ centre.telephone }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ centre.responsable }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ centre.horaire }}</td>
            </tr>
            <tr>
                <th>HoraireApresMidi</th>
                <td>{{ centre.horaireApresMidi }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ centre.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_centre_index') }}">back to list</a>

    <a href="{{ path('app_admin_centre_edit', {'id': centre.id}) }}">edit</a>

    {{ include('admin_centre/_delete_form.html.twig') }}
{% endblock %}
